{% set name = "aodhclient" %}
{% set version = "3.12.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 1956f73b6b99a85544de0abf56ae02f23e7f349a9b83698f0e8e6890c0aad6a4

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - aodh = aodhclient.shell:main

requirements:
  host:
    - pbr
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - python >={{ python_min }}
    - pbr >=1.4
    - cliff >1.16.0
    - osc-lib >=1.0.1
    - oslo.i18n >=1.5.0
    - oslo.serialization >=1.4.0
    - oslo.utils >=2.0.0
    - osprofiler >=1.4.0
    - keystoneauth1 >=1.0.0
    - pyparsing

test:
  requires:
    - python {{ python_min }}
  imports:
    - aodhclient
  commands:
    - aodh -h

about:
  home: https://docs.openstack.org/python-aodhclient/latest/
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: Python client library for Openstack Aodh
  dev_url: https://docs.openstack.org/infra/manual/developers.html

extra:
  recipe-maintainers:
    - tschoonj
